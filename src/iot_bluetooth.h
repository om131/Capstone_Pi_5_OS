// Header guard - prevents multiple inclusion
#ifndef IOT_BLUETOOTH_H
#define IOT_BLUETOOTH_H

// Function declarations (prototypes)
int bluetooth_app(int server_id);
int cloud_forwader(int cliend_id);

#endif